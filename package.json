{
  "name": "threadwork-cc",
  "version": "0.3.3",
  "description": "Production-grade AI coding workflow tool — spec-driven orchestration, hook-enforced quality gates, token budgeting, and structured session handoffs",
  "type": "module",
  "main": "./lib/index.js",
  "bin": {
    "threadwork": "./bin/threadwork.js"
  },
  "scripts": {
    "setup": "node scripts/setup.js",
    "test": "node --test tests/unit/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "test:all": "node --test tests/**/*.test.js",
    "check": "node --check lib/*.js hooks/*.js bin/*.js"
  },
  "keywords": [
    "claude",
    "claude-code",
    "ai-workflow",
    "spec-driven",
    "quality-gates",
    "token-budget",
    "session-handoff"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "gray-matter": "^4.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "bin/",
    "hooks/",
    "lib/",
    "install/",
    "templates/"
  ]
}
